TODO:
* with arguments provides line, word and byte count and a total for multiple files


DONE:
* should handle default arguments for a single file
* should handle a single file with arguments provides line
* with arguments provides line, word and byte count and a total for multiple files
* should provide the number of lines for a single file
* should count lines and words when -lw is specified
* should count lines and words when -wl is specified
* should count lines and bytes when -lc is specified
* should count lines and bytes when -cl is specified
* should count words and bytes when -wc is specified
* should count words and bytes when -cw is specified